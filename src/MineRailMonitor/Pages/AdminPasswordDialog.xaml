<Window x:Class="MineRailMonitor.Pages.AdminPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="管理员验证" Width="360" SizeToContent="Height" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Foreground="{StaticResource TextPrimaryBrush}">
    <Border x:Name="DialogBorder"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource PrimaryBlueBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="6,6,0,0"
                    MouseLeftButtonDown="OnHeaderMouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="管理员验证"
                               Margin="16,0,48,0"
                               VerticalAlignment="Center"
                               FontSize="13"
                               FontWeight="SemiBold" />
                    <Button Content="×"
                            Width="40"
                            Height="32"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,5,0"
                            Padding="0"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Click="OnCloseClick" />
                </Grid>
            </Border>

            <Border Grid.Row="1" Padding="24,20,24,22">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="密码" />
                        <PasswordBox x:Name="PasswordInput" Margin="0,6,0,0"
                                     HorizontalContentAlignment="Center"
                                     VerticalContentAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="ErrorText" Grid.Row="1" Text="管理员密码错误" Foreground="#FF8795"
                               Visibility="Collapsed" Margin="0,10,0,0" />
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="确认" Width="76" IsDefault="True" Click="OnConfirmClick" />
                        <Button Content="取消" Width="76" Margin="10,0,0,0" IsCancel="True" Click="OnCancelClick" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
