<Window x:Class="MineRailMonitor.Pages.DatabaseRecoveryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据库启动检查"
        Width="760"
        SizeToContent="Height"
        MaxHeight="780"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource TextPrimaryBrush}"
        Closing="OnDialogClosing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml" />
                <ResourceDictionary Source="../Styles/Typography.xaml" />
                <ResourceDictionary Source="../Styles/Cards.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="DialogCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="30" />
                <Setter Property="MinHeight" Value="30" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
            </Style>
            <Style x:Key="DialogLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource FieldLabelStyle}">
                <Setter Property="Margin" Value="0,0,10,0" />
            </Style>
            <Style x:Key="DialogValueStyle" TargetType="TextBlock" BasedOn="{StaticResource FieldValueStyle}">
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border x:Name="DialogBorder"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource PrimaryBlueBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="6,6,0,0"
                    MouseLeftButtonDown="OnHeaderMouseLeftButtonDown">
                <Grid Margin="16,0,6,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="4" Height="24" Margin="0,0,10,0" Background="{StaticResource PrimaryBlueBrush}" CornerRadius="2" />
                        <TextBlock Text="数据库启动检查" Style="{StaticResource SectionTitleStyle}" />
                    </StackPanel>
                    <Button Content="×"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Style="{StaticResource DialogCloseButtonStyle}"
                            Click="OnExitClick"
                            AutomationProperties.Name="退出程序" />
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" MaxHeight="670" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="24,20,24,18">
                    <TextBlock x:Name="StateTitleText" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="StateSummaryText" Margin="0,7,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />

                    <Border x:Name="RecoveryWarningBorder"
                            Margin="0,16,0,0"
                            Padding="12,10"
                            Background="#5B4812"
                            BorderBrush="{StaticResource WarningBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <TextBlock Text="恢复后，备份时间之后产生的历史记录可能丢失。"
                                   Foreground="{StaticResource WarningBrush}"
                                   TextWrapping="Wrap" />
                    </Border>

                    <Border x:Name="HealthSummaryPanel" Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="检查摘要" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,12" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="145" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="145" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="生产数据库路径" Style="{StaticResource DialogLabelStyle}" />
                                <TextBlock x:Name="DatabasePathText" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource DialogValueStyle}" />
                                <TextBlock Text="quick_check" Grid.Row="1" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="QuickCheckText" Grid.Row="1" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,12,0" />
                                <TextBlock Text="integrity_check" Grid.Row="1" Grid.Column="2" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="IntegrityCheckText" Grid.Row="1" Grid.Column="3" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="foreign_key_check" Grid.Row="2" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="ForeignKeyCheckText" Grid.Row="2" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,12,0" />
                                <TextBlock Text="错误类型" Grid.Row="2" Grid.Column="2" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="ErrorTypeText" Grid.Row="2" Grid.Column="3" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="错误码" Grid.Row="3" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="ErrorCodeText" Grid.Row="3" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,12,0" />
                                <TextBlock Text="错误码名称" Grid.Row="3" Grid.Column="2" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="ErrorCodeNameText" Grid.Row="3" Grid.Column="3" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                            </Grid>
                            <TextBlock Text="错误信息" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,3" />
                            <TextBlock x:Name="ErrorMessageText" Style="{StaticResource DialogValueStyle}" />
                        </StackPanel>
                    </Border>

                    <Border x:Name="CandidatePanel" Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="可用恢复备份" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="候选已由启动门禁完成本次完整校验。" Margin="0,5,0,10" Foreground="{StaticResource TextSecondaryBrush}" />
                            <ListBox x:Name="CandidateList" Height="130" SelectionChanged="OnCandidateSelectionChanged">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="4,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:Name="CandidateTimestampText" Text="{Binding LocalTimestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" />
                                            <TextBlock x:Name="CandidatePathText" Grid.Column="1" Text="{Binding Path}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <TextBlock x:Name="NoCandidateText" Text="数据库损坏且未找到可用备份" Margin="0,12,0,0" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border x:Name="SchemaPanel" Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="数据库实际版本" Style="{StaticResource DialogLabelStyle}" />
                            <TextBlock x:Name="ActualSchemaVersionText" Grid.Column="1" Style="{StaticResource DialogValueStyle}" />
                            <TextBlock Text="当前应用支持版本" Grid.Row="1" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                            <TextBlock x:Name="SupportedSchemaVersionText" Grid.Row="1" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                        </Grid>
                    </Border>

                    <Border x:Name="InterruptedPanel" Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="系统检测到上一次恢复流程未完整结束。" />
                            <TextBlock Text="在恢复完成前不会启动正常业务。" Margin="0,6,0,12" Foreground="{StaticResource WarningBrush}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="SourceBackupPath" Style="{StaticResource DialogLabelStyle}" />
                                <TextBlock x:Name="SourceBackupPathText" Grid.Column="1" Style="{StaticResource DialogValueStyle}" />
                                <TextBlock Text="CorruptBundlePath" Grid.Row="1" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="CorruptBundlePathText" Grid.Row="1" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="StagingPath" Grid.Row="2" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="StagingPathText" Grid.Row="2" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="StartedAt" Grid.Row="3" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="StartedAtText" Grid.Row="3" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="BundleManifestSha256" Grid.Row="4" Style="{StaticResource DialogLabelStyle}" Margin="0,10,10,0" />
                                <TextBlock x:Name="BundleManifestSha256Text" Grid.Row="4" Grid.Column="1" Style="{StaticResource DialogValueStyle}" Margin="0,10,0,0" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border x:Name="RecoveryStateErrorPanel" Margin="0,16,0,0" Padding="12,10" Background="#5B1927" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="无法确认上一次数据库恢复状态。" Foreground="{StaticResource DangerBrush}" />
                            <TextBlock Text="为避免覆盖现有数据，系统不会继续启动。" Margin="0,6,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <TextBlock x:Name="AdminVerificationHint" Text="执行恢复前需要管理员验证" Margin="0,16,0,0" Foreground="{StaticResource WarningBrush}" />
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#051A2C" BorderBrush="{StaticResource GridLineBrush}" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button x:Name="RecoverButton" Content="恢复数据库" Width="108" Margin="0,0,8,0" Click="OnRecoverClick" AutomationProperties.Name="恢复数据库" />
                    <Button x:Name="ResumeButton" Content="继续恢复" Width="92" Margin="0,0,8,0" Click="OnResumeClick" AutomationProperties.Name="继续恢复" />
                    <Button x:Name="RetryButton" Content="重试" Width="72" Margin="0,0,8,0" Click="OnRetryClick" AutomationProperties.Name="重试" />
                    <Button x:Name="OpenDataDirectoryButton" Content="打开数据目录" Width="112" Margin="0,0,8,0" Click="OnOpenDataDirectoryClick" AutomationProperties.Name="打开数据目录" />
                    <Button x:Name="ExitButton" Content="退出程序" Width="88" Click="OnExitClick" AutomationProperties.Name="退出程序" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
