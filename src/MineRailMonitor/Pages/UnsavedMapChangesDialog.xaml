<Window x:Class="MineRailMonitor.Pages.UnsavedMapChangesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="地图标注"
        Width="430"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource CardBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}">
    <Border Padding="22" BorderBrush="{StaticResource PrimaryBlueBrush}" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="地图标注" Style="{StaticResource SectionTitleStyle}" FontSize="16" />
            <TextBlock x:Name="MessageText"
                       Text="地图标注存在未保存修改。"
                       Margin="0,16,0,20"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondaryBrush}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存并退出" Width="92" Margin="0,0,8,0" Click="OnSaveAndExitClick" />
                <Button Content="放弃修改" Width="82" Margin="0,0,8,0" Click="OnDiscardClick" />
                <Button Content="取消" Width="58" Click="OnCancelClick" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
