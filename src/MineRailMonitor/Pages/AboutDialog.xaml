<Window x:Class="MineRailMonitor.Pages.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MineRailMonitor.Pages"
        Title="关于 MineRailMonitor"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource TextPrimaryBrush}"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="AboutButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#082A45" />
            <Setter Property="BorderBrush" Value="#168DCA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="AboutButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="AboutButtonBorder" Property="BorderBrush" Value="#15B9F0" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="AboutButtonBorder" Property="Opacity" Value="0.78" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="AboutButtonBorder" Property="Opacity" Value="0.45" />
                                <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AboutPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AboutButtonStyle}">
            <Setter Property="Background" Value="#087FBA" />
            <Setter Property="BorderBrush" Value="#15B9F0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource PrimaryBlueBrush}"
            BorderThickness="1"
            CornerRadius="7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#06233D"
                    BorderBrush="{StaticResource GridLineBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="7,7,0,0"
                    MouseLeftButtonDown="OnHeaderMouseLeftButtonDown">
                <Grid Margin="18,0,8,0">
                    <TextBlock Text="关于 MineRailMonitor"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontWeight="SemiBold" />
                    <Button Content="×"
                            Width="36"
                            Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Padding="0"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            FontSize="20"
                            Style="{StaticResource AboutButtonStyle}"
                            Click="OnCloseClick" />
                </Grid>
            </Border>

            <Border Grid.Row="1" Padding="30,24,30,20">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Image Source="/MineRailMonitor;component/Assets/Icons/app-logo.png"
                               Width="84"
                               Height="84"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality" />
                        <StackPanel Grid.Column="1" Margin="22,2,0,0">
                            <TextBlock Text="矿山轨道运输监控系统" FontSize="22" FontWeight="SemiBold" />
                            <TextBlock Text="MineRailMonitor" Foreground="{StaticResource TextSecondaryBrush}" FontSize="15" Margin="0,7,0,0" />
                            <TextBlock Text="安全运输 · 精准感知 · 智能运维" Foreground="{StaticResource PrimaryBlueBrush}" FontSize="12" Margin="0,14,0,0" />
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,24,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="版本信息" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{Binding DisplayVersion, RelativeSource={RelativeSource AncestorType={x:Type local:AboutDialog}}}" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock Grid.Row="1" Text="运行环境" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,8,0,0" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text=".NET Framework 4.8" FontSize="12" Margin="0,8,0,0" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" BorderBrush="{StaticResource GridLineBrush}" BorderThickness="0,1,0,0" Padding="24,0">
                <Grid>
                    <TextBlock Text="© 2026 MineRailMonitor" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" VerticalAlignment="Center" />
                    <Button Content="确定"
                            Width="88"
                            Height="34"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Style="{StaticResource AboutPrimaryButtonStyle}"
                            IsDefault="True"
                            Click="OnCloseClick" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
