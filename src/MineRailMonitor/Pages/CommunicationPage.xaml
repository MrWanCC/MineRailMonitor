<UserControl x:Class="MineRailMonitor.Pages.CommunicationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="通信调试" Style="{StaticResource PageTitleStyle}" />
        <TextBlock x:Name="ListenerStatusText" Grid.Row="1" Margin="0,8,0,14"
                   Foreground="{StaticResource TextSecondaryBrush}" />
        <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="最近 RX" FontSize="18" FontWeight="SemiBold" />
                <TextBlock x:Name="LatestRxText" Grid.Row="1" Margin="0,10,0,12"
                           Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" />
                <ListBox x:Name="RxListBox" Grid.Row="2" Background="Transparent"
                         BorderThickness="0" Foreground="{StaticResource TextPrimaryBrush}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
